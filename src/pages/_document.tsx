import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
  return (
    <Html className="scrollbar-thin scrollbar-track-primary-content scrollbar-thumb-primary scrollbar-thumb-rounded-full">
      <Head />
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
